[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
# Explicitly install ansible-lint, as molecule doesn't appear to
ansible-lint = "*"
molecule = { version = "*", extras = ["lint","docker"]}

[packages]
ansible-core = "*"

[requires]
python_version = "3.9"
