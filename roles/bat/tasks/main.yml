---
# Package divert required due to a package name clash
- name: Divert and rename batcat for Debian based distros
  community.general.dpkg_divert:
    path: /usr/bin/bat
    divert: /usr/bin/batcat
    rename: yes
    holder: branding
  when: ansible_os_family == "Debian"

- name: Install
  package:
    name: bat
    state: present
  become: yes
